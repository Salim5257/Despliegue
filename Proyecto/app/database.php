<?php

    /* Conexión a la base de datos */

    $connection = mysqli_connect(
        'localhost',
        'debianDB',
        'debianDB',
        'proyecto'
    );

    // if($connection) {
    //     echo "Database is connected";
    // }

?>
